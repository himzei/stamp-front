import { Text } from "@chakra-ui/react";
import Layout from "../components/Layout";

export default function Profile() {
  return (
    <Layout hasTabBar hasTitle title="프로필">
      <Text>프로필</Text>
    </Layout>
  );
}
